{
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @nexron/api build",
  "outputDirectory": ".next"
}
