{
  "name": "nexron-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Modular free-first AI workspace.",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @nexron/web dev",
    "build": "pnpm --filter @nexron/api build && pnpm --filter @nexron/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "start": "pnpm --filter @nexron/web start"
  }
}
